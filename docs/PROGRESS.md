# Progress

## CURRENT MILESTONE
**Chrome brain animation no longer uses the 10 Hz Blink cap.** The measured cause was browser-specific: Firefox paints the full cloud at 30 Hz, while Chrome/ANGLE/Metal needed a 10 Hz limit to protect the worker. Chrome now targets 30 Hz with a stable one-sixth display sample, maps only sampled activity, and permits these short draws during worker waits. Full geometry, model activity, 2× backing resolution, and full-cloud capture/population views remain. A separate brain-paint FPS counter makes the result visible; the final production-Chrome combat gate recorded 53 game / 37 arena-render / 22 brain-paint / 27 neural fps. This more than doubles the old brain cadence on this host while reducing point work; the observed 22 fps remains below the 30 Hz target. See [BENCHMARKS.md](BENCHMARKS.md) and [ACTIVITY_DISPLAY.md](ACTIVITY_DISPLAY.md).

**Public GitHub presentation refreshed for the LinkedIn post.** README now leads with the playable game, a checked-in Chrome gameplay/brain screenshot, clear measured-versus-modeled boundaries, controls, a runtime diagram, concise developer setup, and links to detailed evidence. `data/README.md` no longer describes the default V2 controller as synthetic. Regenerable sprite QA previews are removed from the tracked tree and ignored. Documentation links and whitespace were checked. On 2026-09-23, native 5/5, TypeScript 99/99, policy/metric 2/2, Python 17/17, production build, and Chrome 29/29 passed. Full `./scripts/verify.sh` stopped at WASM rebuild because Emscripten 4.0.15 is absent in this shell; browser tests used the retained WASM artifacts.

**Chrome ANGLE soma draws no longer starve the neural loop on M-series Metal.** Interactive activity points are opaque squares (no circular `discard`), size-capped, and painted at 10 Hz on Blink while a neural request is in flight the soma pass is skipped; the display loop calls `Session.tick()` before any canvas work. Soft circles remain for capture/cinematic and population highlight. 2× backing-store and 60 Hz sim / 30 Hz model targets are unchanged.

**Chrome brain presentation no longer restyles the WebGL canvas every frame.** Live lean/pitch/bloom/breathe now drive the same camera uniforms as capture (yaw/pitch/roll/zoom/pan) inside the draw path for both WebGL and Canvas; CSS `transform` on the 139k-point canvas is gone. Uniform locations are cached once, the context requests `powerPreference: high-performance`, and point sprites use a squared-radius discard with matched highp precision (Chrome rejects mixed vertex/fragment uniform precision). Resolution, 60 Hz game target and 30 Hz model step are unchanged. Production Chrome stationary-combat gate: **52 game / 60 render / 26 neural fps**.

**Low-end interactive scheduling and presentation work reduced without lowering resolution.** Worker latency now counts toward the current 60 Hz frame budget instead of being followed by an artificial 16.7 ms idle interval; accumulated wait debt is capped at one frame, preserving sequential deterministic steps without catch-up bursts. Held 30 Hz model frames omit a redundant 166,700-byte activity transfer, React scoreboard/signals publish at 10 Hz, and the independent soma view paints at 15 Hz on phones/≤4-core devices (30 Hz on capable desktops) while every canvas retains the 2× backing-store cap. Static population inspection hides live signal overlays and remains pixel-stable. Focused production Chrome improved the stationary-combat gate from 37–38 fps to 50 game / 60 render / 25 neural fps; the full suite later recorded 53 / 60 / 27.

**Controls strip drops the “Three stocks / One tiny rival” heading.** Fox controls keep the eyebrow, key legend, and reset/pause actions; unused global `h2` styles were removed and key spacing adjusted. Chrome still asserts the controls region under the scoreboard.

**Phone layout drops the under-stage Fox controls strip.** On ≤720px the match panel ends at the scoreboard; stick/HIT overlays remain on the arena. Desktop still fills the column gap under the scoreboard with the controls legend. Chrome asserts the controls region is hidden on 390×844 and that the scoreboard closes the match panel.

**Fox controls fill the empty match-column gap under the scoreboard.** Guidelines moved out of the lower-grid strip into `.arena-panel` below the HUD so the stretched left column no longer shows a blank beige band beside the taller anatomy panel. Sensory/motor bars remain bottom-corner overlays on the full-width soma canvas. Chrome asserts controls sit under the scoreboard inside the match panel.

**Anatomy stage is full-width brain with bottom signal overlays.** Sensory/motor bars sit at the bottom corners over the soma canvas instead of opaque side columns, so the upper brain cloud is never clipped by dark flanks. Motor column width is fixed and APPLIED text ellipsizes so long action strings do not resize the panel; the zero/higher-activity legend sits at the top-right clear of the motor stack. `.brain-stage` background is transparent; only `.brain-view` keeps the activity backdrop. Fight height remains `clamp(220px, 34dvh, 280px)` and `.brain-view` 310px. Asset DPR stays at the desktop 2× cap; phone game draw remains 30 fps, constrained soma draw is 15 fps, and React HUD is 10 Hz. Visibility-hide pauses still resume automatically. Chrome regression asserts fight ≥220px, brain 310px, bottom-corner bars, stable motor width, top-right activity key, and no horizontal overflow.

**Fox input sampled at neural apply time (latency cut, no model/feel change).** Game ticks still wait on the MaleCNS worker, but human Fox axis/buttons are read when the reply is applied instead of when the observation was posted. Keys pressed during a slow neural frame no longer stay locked to the stale pre-wait sample. Capture `ScriptedInput` still advances once per applied tick. Architecture note updated; focused session test covers late KeyD/KeyJ during an in-flight request.

**Implementation-only neural hot-path cuts (no model/feel change).** MaleCNS worker posts transferable activity/sensory/motor copies while keeping cached frames attached for pause retries; activity packing runs only on 30 Hz model steps (WASM `rate_pack`, same `min(255, round(rate/128))` formula) and is reused on held game frames; WASM input writes clear/set only prior drive indices instead of copying the full input vector; display uses a 256-entry log LUT, skips unchanged WebGL activity uploads, and publishes React scoreboard/signal state at ~10 Hz. Full graph, rate dynamics, 60 Hz game target, sensors/readout and 139k somas are unchanged. Long-match after-timing provenance was regenerated for new controller/WASM hashes; the scheduler follow-up now clears the host FPS gate.

**Gameplay-coupled brain presentation makes the live soma cloud feel responsive.** The display now leans toward the Fox–Fly relationship, tracks Fly vertical motion, blooms with current motor drive, and pulses on attack/hitlag edges, with subtle motion only while play is running. All motion is renderer-only; pause freezes it and the optional controller-population inspection remains fixed. WebGL and Canvas share pose/size response. One focused mapping test, production build, and Chrome gameplay smoke test pass; screenshot reviewed. See ACTIVITY_DISPLAY.md.

**LinkedIn capture mode for post-brain-reveal gameplay.** `/capture/?shot=` now provides six 1080×1350 deterministic edit plates (reveal, approach, live brain, Fly response, beat-aligned impact, synchronized split), five gameplay cameras, four restrained brain cameras, renderer-only 2.5D parallax and clean ProRes/H.264 export. The real V2 hero replay produces Fly attack at state frame 37 and an 8% hit at 42; the impact plate lands that hit at output frame 28. The `approach` plate holds ticks 0–88 and forces Fly sim input idle after tick 42 so Fox’s knockback fall reads without a follow-up walk. Evidence: `data/hero-replay.json` and `data/capture-sequences.json`.

**The Clearing stage map + nebula background replace geometric arena art (visual-only).** Authored floating-platform PNG is keyed to transparent alpha and drawn so the measured top surface maps to the sim walkable plane at y=0 / ±68. A separate 1024×576 backdrop draws screen-space with object-fit cover behind the world transform. The fixed view is calibrated at a 220-world-unit span and keeps the complete plate visible; it is explicitly not the inspected Melee subject-tracking camera. Collision, blast zones and controller sensors are unchanged. Source: `assets/stages/`; runtime: `web/public/assets/stages/`.

**Fox/Fly sprite sheet rendering replaces geometric placeholder fighters (visual-only).** Authored reference sheet extracted into normalized transparent PNGs under `web/public/assets/fighters/{fox,fly}/`. Canvas draw path preloads the sprite sets, maps Idle/Walk/Dash/Run/JumpSquat/Air/Attack phases (plus a short visual landing hold), and uses the extractor's body/feet anchors so wide frames and facing flips remain fixed on the sim root. Walk cadence advances by horizontal travel (Fox 12 / Fly 10 world units per cycle) instead of a fixed tick FPS, reducing foot skating; physics untouched. Dev overlay: `?debugSprites=1`. Extraction: `scripts/extract_fighter_sprites.py`. Remaining art limit: `walk-4` is a weak opposite-passing pose for both fighters.

**LinkedIn MaleCNS brain cinematic renderer added (isolated from gameplay).** Route `/cinematic/` loads measured MaleCNS somas (139,662) via the existing connectome package; default activity is explicitly labeled `SYNTHETIC_CINEMATIC_TIMELINE` with an `ActivitySource` adapter for future real model buffers. Deterministic 1080×1350 @ 60 fps PNG export (Playwright + canvas.toDataURL) and ffmpeg ProRes/H.264 scripts verified locally: 150 frames, determinism hash check, preview reports 60 fps. Game build still produces both `dist/index.html` and `dist/cinematic/index.html`. See docs/CINEMATIC.md.

**V2 attack/recovery timing corrected locally; not published.** A timing trace measured four game frames from attack sensor onset to decoder pulse; with five authored startup frames, the new reach sensor projects relative motion nine frames. Rejected jump/attack pulses no longer consume cooldown, recovery drive can accumulate during hitstun, attacks are withheld during recovery, and a common 42-unit safety margin leaves 26 units before the local platform edge. The corrected 12-bout assay completes in 7,763 frames / 129.38 seconds: both shuttle timeouts and both rushdown losses become wins, while stationary, cross-up and edge-bait retain their outcome, surviving-stock and damage floors. Native replay agrees for every bout and the full neural reset replay is identical. `./scripts/verify.sh` exited 0: native 5/5, TypeScript 49/49, policy/metric 2/2, Python 17/17, Chrome 24/24. See MATCH_EVALUATION.md, the preserved `data/long-matches.json` baseline, and `data/long-matches-after-timing.json`.

**Soma display and optional controller-population view verified locally.** V2 holds latest activity with fixed log display scale; the optional static population view highlights exact drive/readout membership, not activation. Coverage: drive 9,162 positioned / 9,201 total; readout 2,011 / 2,022. Full 600-frame activity/motor hash remains unchanged. Desktop/mobile screenshots reviewed. See ACTIVITY_DISPLAY.md. These verified display changes are published in the current Site release.

**Controller V2 deployed and anonymously verified.** Stable WASM rate dynamics, recovery/reach sensors and calibrated readout replace saturated LIF gameplay. Full-graph checks track both directions, reverse without reset, attack in range and recover. Stationary-opponent damage improves 0→32, jumps 13→4 and fastfall frames 581→2. `./scripts/verify.sh` exited 0 (native 5/5 including rate, TypeScript 40/40, production Chrome 22/22). The 2026-09-13 deployment record covers 223 static files; a fresh anonymous Chrome received the V2 WASM/calibration assets, showed the Recovery need sensor, dealt 8% damage to a stationary Fox, and paused/reset without errors.

Current public URL: https://fox-vs-fly.pages.dev/ (provided 2026-09-23; not verified from this environment)

## DONE
- Reworked the public README as a concise landing page, added a local Chrome screenshot, corrected default-controller wording in `data/README.md`, checked the linked documentation paths, and removed 57 regenerable sprite QA images from source control.
- On Blink: opaque square interactive soma points (no circular discard), 10 Hz brain paint, skip soma while neural-pending, rAF-driven `Session.tick()` before canvas work, and draw-time backoff — keeps 2× DPR and sim cadence while unblocking Chrome ANGLE/Metal.
- Moved gameplay brain lean/pitch/bloom off CSS canvas transforms into WebGL/Canvas camera uniforms; cached GL uniforms and requested high-performance WebGL2 — targets Chrome ANGLE restyle cost that Firefox largely ignored.
- Removed artificial post-worker pacing idle, capped neural-wait debt at one frame, and immediately pipelines the next due request without skipping simulation frames.
- Split game/soma/React presentation cadences, retained 2× canvas resolution, omitted redundant held-frame activity transfers, and added focused profile/session coverage.
- Removed the “Three stocks / One tiny rival” heading from the Fox controls strip; eyebrow and key legend remain.
- Hid the under-stage Fox controls strip on phone (≤720px); match panel ends at the scoreboard while stick/HIT overlays stay on the arena. Desktop column-fill under the scoreboard unchanged.
- Moved Fox controls into the match panel under the scoreboard so they fill the leftover column height next to the anatomy panel; removed the empty lower-grid strip.
- Kept anatomy sensory/motor as transparent bottom-corner overlays on a full-width soma canvas (no opaque side columns clipping the upper brain).
- Moved sensory/motor bars into the anatomy panel over the soma canvas.
- Compacted phone chrome and raised fight height (`34dvh` arena clamp) while restoring the brain panel to 310px; phone presentation keeps 30 fps game / 15 fps soma / 2× DPR / 10 Hz HUD with visibility auto-resume.
- Fox input is sampled when the neural reply is applied, not when the observation is posted, so presses during a slow worker frame are no longer frozen to the stale request-time sample. Capture scripting still advances once per applied tick.
- Implementation-only V2 hot-path: transferable worker frames, pack-once-per-model-step (+ WASM pack), sparse WASM input writes, log LUT / dirty WebGL upload, throttled sensory/motor React bars (snapshot/tick stay live). Dynamics, packing formula, sensors and readout unchanged; fixtures cover pack reuse and sparse drive clearing.
- Made the gameplay brain feel alive without touching neural/game state: smoothed opponent-relative yaw, Fly-velocity pitch, motor-linked bloom, attack/hitlag pulses, live color grade and a paused/static-inspection freeze. Added Canvas parity for pose/size and one focused renderer mapping test.
- Fixed phone auto-pause: removed spurious `window.blur` pause (visibility hide only), stopped accruing schedule debt while waiting on the neural worker, and made HIT a non-focusing control so iOS does not scroll/blur mid-match. A 390×844 Chrome regression now dispatches `window.blur` during live play and verifies that the match stays running and its tick advances.
- Phone touch controls: left analog stick (move; up jump / down fastfall) and right HIT overlay on narrow/coarse-pointer viewports; merges with keyboard via PlayerInput.
- Walk sprite cadence now advances by horizontal travel (authored stride Fox 12 / Fly 10) instead of tick×FPS, so steps track ground speed and skate less; phase resets when leaving walk. Physics unchanged. `walk-4` art remains a weak opposite-passing pose.
- Extended LIF sign/timestep/retention sensitivity with 12 predeclared conditions, 108 independently reset pulse/mapping trials per pass and exact reverse-condition/membership/stimulus replay. Retained `data/male-cns-v1.0.sensitivity.json` pins graph/annotation/source hashes and reproduces prior long-pulse and amplitude-500 mapping anchors. All three shuffle seeds retained; zero-sign readouts checked analytically. Arbitrary timestep scaling and its changed edge delay remain explicit MODEL_ASSUMPTION, with no biological defaults or gameplay changes. See NEURAL_MODEL.md and POPULATION_MAPPING.md.
- Added LinkedIn capture mode: searched deterministic Fox timelines against live V2, locked `hero-sequence` (feint-L-then-R) markers, chrome-free `?capture=` UI with game/brain views, ScriptedInput driver, `window.__CAPTURE__`, unit tests and CAPTURE.md. Music remains an editing-only concern.
- Replaced flat arena fill with authored Clearing nebula background: preloaded JPEG, screen-space object-fit cover, dark fallback if unload lags; stage plate and fighters draw above it.
- Replaced geometric platform drawing with authored Clearing stage map art: transparent PNG preloaded at boot, top-surface pixels aligned to sim ±68 / y=0, geometric fallback if unload lags, collision untouched.
- Replaced geometric fighter drawing with sheet-extracted Fox/Fly sprites: consistent per-fighter canvas/anchor, sim-driven animation priority, horizontal flip without duplicate assets, sprite preload before PLAY, and optional `?debugSprites=1` overlay. Physics/hitboxes unchanged. Draw height accounts for extract `PAD` so opaque feet sit on the sim root / stage top.
- Inspected pinned camera/stage references before visual calibration. Added pure fixed-view projection tests, widened the authored view from 240 to 220 world units, kept the plate inside desktop/mobile canvases, and corrected normalized sprite drawing to use extractor body anchors instead of canvas centers. `MELEE_PORT.md` labels every renderer value as approximate and keeps it separate from simulation collision.
- Corrected V2 moving-target attack and recovery timing from retained traces. Baseline shuttle attributed hits were 1/120 and 0/120 requests; corrected traces are 3/4 and 6/9. Rushdown changes from two losses at zero stocks to wins at two/three stocks. Stationary remains three/three stocks, cross-up meets or improves one/one, and edge-bait remains three/two. Controller source hashes, before/after timing traces and executable baseline floors are retained without biological or general win-rate claims.
- Evaluated the original V2 across 12 retained scripted bouts (five combat policies and a 120-second nonattacking shuttle, two phase seeds each). Recorded outcomes, damage, stocks, action requests, recovery excursions, activity samples, stock-loss controller state and source hashes. Native replay agrees for all 23,834 baseline frames and a complete neural reset replay is identical.
- Isolated LinkedIn brain cinematic (`web/cinematic/`, `web/src/cinematic/`): Three.js Points over measured MaleCNS geometry, absolute-frame timeline, preview controls, Playwright PNG exporter, ffmpeg ProRes master + H.264 preview. Activity honesty tags and ExternalBufferActivity adapter documented in docs/CINEMATIC.md.
- Added optional, off-by-default controller-population highlight with blue drive/orange readout and exact positioned/unpositioned counts from the actual rate mapping. One startup role-byte transfer; no activity mutation, new drive or graph arrays on UI thread. Static membership, caption, legend and reset behavior tested in WebGL/Canvas.
- Diagnosed high active-count/dim soma mismatch with deterministic approach/attack samples in data/activity-evidence.json and docs/ACTIVITY_DISPLAY.md. V2 display holds latest continuous rates on a fixed log scale; zero remains zero. Added packing, mapping, zero/reset and paused pixel regression checks. Neural drive, calibration and dynamics unchanged; VNC weakness and missing sensory soma coverage explicitly documented.
- Clarified Melee reference policy: Nintendo disc/Dolphin/decomp are developer-offline tools for fixtures only; never browser/player runtime deps. Documented in AGENTS.md, MELEE_PORT.md, ARCHITECTURE.md; gitignore covers common disc formats.
- Public static deployment with all 219 runtime assets. Anonymous Chrome reaches live neural gameplay, moves Fox, pauses/resets and reports no page errors.
- Independent scalar GameBatch API, per-frame serial equivalence at all five sizes, isolated resets and atomic shape validation. Deterministic warmup plus three-repeat benchmarks recorded in data/batch-benchmark.json.
- Source-informed sticky short hop and launch-frame physics skip, authored heights, exhaustive button-release fixtures, and serialized latch in deterministic hashes.
- Closed neural/game loop, automatic graph preparation, actual spike HUD, explicit dummy route, cached pause retries and reset generations. Connectivity-removal and pulse fixtures; 600-frame real-graph reset/native/WASM agreement. See NEURAL_GAMEPLAY.md.
- Inspected pinned Melee, DoomFly, HAL sources and official MaleCNS release/download pages before implementation.
- Persistent AGENTS.md, architecture, fidelity matrix, scientific boundaries and developer README.
- Deterministic integer C++17 simulation: two fighters, one platform, movement, walk/dash/run, jumpsquat, jump/double jump, drift, gravity/terminal/fastfall, traction, landing, attack phases, hit detection, damage, knockback, hitlag/stun, trades, stocks, respawn immunity, victory/draw.
- Native CMake tests, serialized replay and benchmark. Same core compiled with Emscripten 4.0.15.
- React/Vite/TypeScript frontend with original geometry-based fighter art, keyboard controls, HUD, pause/reset and match-end flow.
- Explicit synthetic demo: Worker DummyBrain with explicit sensory encoder and motor decoder. Displayed output values are exactly those decoded for the WASM input.
- BrainFrame interface, typed-array geometry/activity, raw WebGL2 point renderer and tested Canvas fallback. Display persistence is isolated from the controller.
- Fixed-step scheduler independent of rAF; stale-response rejection, reset epochs, blur/tab pause and worker failure handling.
- Production static bundle at web/dist includes optional ~76 MiB graph transport plus 2.2 MB geometry; WASM itself 4,544 bytes.
- Real official v1.0 annotation, NT and weighted-connectivity inputs downloaded (~1.11 GB total), SHA-256 locked in data/male-cns-v1.0.sources.json. No EM/synapse-coordinate bulk files.
- Developer-only Arrow/NumPy importer produces sorted CSR_V2, exact IDs, measured positive weights, annotation/NT dictionaries, original and normalized soma coordinates, and visual-to-graph indices.
- Explicit node/edge/contact retention accounting; missing positions and NT do not remove graph neurons. All retained weight-one edges and autapses preserved.
- Real artifact: 166,700 neurons; 25,582,938 directed edges; 139,662 soma positions; 27,038 graph nodes without soma positions. Source annotation rows: 211,577 (not a biological neuron total).
- Validated real output at data/generated, 218,702,759 bytes (~209 MiB). Raw/generated binaries and project venv are ignored. Compact data/male-cns-v1.0.report.json records evidence and output hashes.
- Six new importer tests plus six existing artifact tests: exact IDs, missing data, sparse direction, coalescing/overflow, loss accounting, source corruption and deterministic output-array hashes.
- Compact browser geometry package generated and versioned in web/public/connectome: 2,234,592 binary bytes, no full graph arrays.
- SHA-256/count/dtype/range-validated browser loader; demo-only explicit playable synthetic fallback on missing/corrupt geometry.
- Runtime-sized worker activity over all retained model nodes; visual_indices correctly maps to measured positioned subset; reset clears display persistence.
- Demo UI separates measured anatomy from synthetic activity and displays coverage/counts from package metadata. Offline edge count explicitly labeled; no biological controller claim.
- Desktop/390px mobile screenshots reviewed with measured soma geometry: brain above VNC, no horizontal overflow. No connecting fibers fabricated. Retained local preview responds HTTP 200.

- Added deterministic gzip transport, bounded hash-checked worker CSR loading, exact ID/anatomy identity checks, metadata and structural validation.
- Added on-demand progress, cancellation, failure retry and unload; no large arrays cross the UI thread.
- Real Chrome loaded/validated the graph in 0.959 seconds; 210,664,708 array bytes and 1,728,344 maximum explicit chunk staging bytes. These are not total heap/RSS measurements.

- Added browser-compatible sparse integer LIF with eight semantic/reference/overflow/replay tests and documented assumptions in NEURAL_MODEL.md.
- Full-topology headless benchmark verifies source hashes and repeated spike traces for silent/sparse/dense artificial drive. Model state adds 3,167,300 bytes.

- Browser benchmark performs 1,080 total ticks across three workloads and warmup/measurement/reset replay, yielding every four ticks.
- Tested stop with retained graph, rerun, cross-runtime spike traces and unload during execution; match continues. Results UI screenshot inspected. Model/input arrays total 3,834,100 bytes beyond graph storage; total browser heap is unmeasured.

- Added controlled comparison UI, independent model resets, late-window spike counts and suite validation. Focused fixture/Chrome tests pass; screenshot inspected.
- Transmission-off response agrees with analytical 66,680 spikes; pulse response persists under all-positive signs, documenting need for stability/sign investigation.

- Added 600-tick pulse suite with stronger-leak and sign controls, conserved 60-tick spike bins, last-spike diagnostics and independent reset replay.
- Focused Chrome results: baseline 2,318,764 spikes through tick 599; stronger leak 6,722 spikes ending at tick 11; alternating signs 9,057 spikes through tick 599. No biological interpretation claimed.

- Added verified reproducible annotation candidate report and POPULATIONS.md. Missing soma positions never remove selected sensory neurons. Related tbc labels remain separate.
- Added three fixture tests for exact IDs, missing metadata/positions, reproducibility, corrupted sources and invalid codes/mapping; canonical verification checks the real report byte-for-byte.

- Added annotation mapping core and worker suite: exact superclass/side selectors, normalized readout, fixed-seed membership permutation and excluded-side reporting.
- Chrome confirms audited population sizes and zero disconnected output; reset traces agree. Readout screenshot inspected and limitations documented in POPULATION_MAPPING.md.

- Added independent lateral suite with separate stimulus hashes, reversed-order replay equality and descriptive directional contrast. Same-side analytical fixture and real graph controls pass; screenshot inspected.

- Added 15-condition amplitude/shuffle suite with explicit seeds, per-condition metadata and reverse-order replay. All outcomes retained in data/male-cns-v1.0.mapping-sweep.json.
- Annotated contrast exceeds all three tested shuffles at 250/500/1000, but left input still favors right readout at lower strengths. Raw difference is not a validated bidirectional controller.

- Added lazy diagnostic renderer and transferred full-population activity frames from actual SparseLif spikes; no fake sensory/motor frame values.
- Focused Chrome matches mapped trace 322408360 after reset; clear removes activity/counters, unload disposes worker/view. Screenshot inspected.

## IN PROGRESS
None. The timing-corrected controller and its evidence are verified locally and intentionally not published.

## FAILING
Native rate/game tests (5/5), all 94 TypeScript tests, policy/metric checks (2/2), all 17 Python tests, the production build, and all 29 Chrome tests pass. `./scripts/verify.sh` stops before WASM replay and later checks because Emscripten 4.0.15 is not configured in this shell; the production build and browser suite were run separately against the retained WASM artifact. This is an environment/toolchain limitation, not a test failure in the changed code.

## KNOWN APPROXIMATIONS
Default gameplay activity is the stable rate model over measured wiring with a calibrated readout; the explicit dummy route retains synthetic activity; optional LIF diagnostics remain available. No biologically validated sensory/motor mapping. Geometry is measured somas only, not neurites or synapses; physical units not independently verified. 27,038 retained neurons have no soma position and are excluded only from display. Default graph preparation retains large typed arrays plus the WASM rate copy; graph dynamics now control default gameplay. Canvas fallback is functionally tested but has no 60-fps guarantee at the real point count.
Gameplay remains approximate with authored tuning and shared generic mechanics. No ECB fidelity, DI, shield, grab, ledges, specials, body pushboxes, audio, touch controller, online play, or online matchmaking.

## MELEE FIDELITY STATUS
Jump release/launch and gravity/fastfall structure PARTIALLY_MATCHED; other implemented systems APPROXIMATE; deferred systems NOT_IMPLEMENTED. Nothing is REFERENCE_MATCHED. Native/WASM agreement proves our core consistency, not Melee fidelity. See MELEE_PORT.md.

## FLY CONNECTOME STATUS
Official v1.0 graph processed and structurally validated. Browser renders 139,662 measured soma positions via mapping into 166,700 retained graph indices. The UI reads counts and missing-position coverage from packaged metadata. 25,582,938 connections / 124,177,617 synaptic contacts can be prepared in a separate worker. The default gameplay worker runs MaleCNSBrain V2 (stable rate WASM + calibrated readout); the explicit dummy route retains DummyBrain; historical LIF gameplay remains in male_cns_lif.ts. See FLY_CONNECTOME.md, NEURAL_GAMEPLAY.md and the source lock/report.

## BROWSER STATUS
Production Chrome passes neural + browser suites for automatic neural game preparation, live model activity, neural pause/resume/reset, graph failure without silent fallback, neural Canvas/mobile, approach-and-hit against stationary Fox, stable population pixels, measured geometry, game controls/HUD/reset, WASM/worker failures, WebGL2/Canvas fallback, corrupt-geometry fallback, and 390px layout. The updated production combat gate separately reports **53 game / 37 arena-render / 22 brain-paint / 27 neural fps** with `WebGL2 · reduced-work mode`; the test requires at least 20 brain paints/sec and 50 game frames/sec. Earlier reports did not separate brain-paint cadence from model updates. Desktop Firefox keeps the full-cloud 30 Hz target without Blink sampling. Dummy-controller smoke remains around 59 game fps. Sustained physical low-end-device performance remains untested.

## BENCHMARKS
Browser geometry binaries 2,234,592 bytes; source graph unchanged at 218,702,759 bytes. Latest native benchmark was ~8.70M frames/sec; dummy microbenchmark ~64.6k steps/sec at its 7,200-sample test size. Graph load was 0.993 s with 77.2 ms structural validation, 76.0 MiB logical download and 200.9 MiB arrays. Headless LIF measured 0.705/2.980/11.789 ms per tick for silent/sparse/dense artificial drive; browser LIF measured 0.439/3.029/17.659 ms with dense p95 52.6 ms. These run-specific values make no 60 Hz neural budget or biological performance claim. See BENCHMARKS.md.

## NEXT 3 TASKS
1. Continue Fox fidelity beyond partial jump/gravity behavior: DI, collision and attack/knockback references.
2. Broaden controller evaluation to additional policies, spawn orientations and held-out phases before making robustness claims.
3. Measure sustained mobile/low-end browser fps after the hot-path cuts; keep SharedArrayBuffer / GPU deferred for single-match play.
