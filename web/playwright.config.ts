import {defineConfig} from '@playwright/test';
export default defineConfig({testDir:'./tests',fullyParallel:false,workers:1,timeout:30000,use:{baseURL:'http://127.0.0.1:42873',headless:true,channel:process.env.PLAYWRIGHT_CHANNEL||'chrome',viewport:{width:1440,height:1100},screenshot:'only-on-failure'},webServer:{command:'npm exec vite -- preview --host 127.0.0.1 --port 42873 --strictPort',url:'http://127.0.0.1:42873',reuseExistingServer:false}});
